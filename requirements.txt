# Testing
pytest==8.3.3
httpx==0.27.2

openai>=2.44.0
python-dotenv>=1.2.2
fastapi>=0.138.0
uvicorn>=0.49.0
pydantic==2.12.5

pypdf>=6.19.0
beautifulsoup4>=4.15.0
# OCR for the scanned policy PDFs (D01, D10, D12, D15). Needs the poppler
# `pdftoppm` binary on PATH to render pages: apt install poppler-utils
rapidocr-onnxruntime>=1.2.3
